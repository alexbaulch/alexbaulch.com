import React from 'react';
import Hero from './hero';

export default class AppView extends React.Component {
    render() {
        return (
            <div id="app-view">

            </div>
        );
    }
}